from setuptools import setup

setup(name='lightnet',
      version='0.0.1',
      description='Building blocks for recreating darknet networks in pytorch',
      author='EAVISE',
      packages=['lightnet',
      scripts=[],
)
